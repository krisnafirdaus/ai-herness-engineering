# ── Core runtime ────────────────────────────────────────────────────────────
# The stage logic, state store (SQLite) and local sandbox run on the Python
# standard library; everything below is what the production deployment uses.
# The offline demo (`make demo`) still works without any of it via the builtin
# orchestration fallback.

# Orchestration engine (LangGraph StateGraph drives the run loop; the builtin
# while-loop driver is the zero-dependency fallback: HARNESS_ORCHESTRATOR)
langgraph==1.2.4

# API server + streaming (SSE/WebSocket)
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
httpx==0.28.1          # FastAPI TestClient + API integration tests

# Real LLM providers (optional — the deterministic offline mock needs neither)
anthropic==0.34.2
openai==1.40.0

# Docker sandbox (optional — local isolated-workspace sandbox is the fallback)
docker==7.1.0

# Telemetry export (optional — tracing always works via the SQLite span store)
langfuse==2.44.0

# Redis-backed queue for horizontal worker scaling (optional — DB queue is default)
redis==5.0.7

# Postgres state backend (optional — SQLite is default)
psycopg[binary]==3.2.1
